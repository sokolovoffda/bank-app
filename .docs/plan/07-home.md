# Этап 7 — Home screen

**Оценка:** 3–5 дней (по блокам, не всё сразу)  
**Статус:** ⬜ не начат  
**Цель:** домашний экран из store + реального API; перевод меняет balance в PostgreSQL.

**Готово, если:** после login на `/` видны карта и транзакции с сервера; transfer (или хотя бы top-up) отражается в БД и в UI.

Делать **по блокам**, каждый блок можно сдавать отдельно.

---

## Client

### Блок A — CardInfo

- [ ] Компонент карты: баланс, номер (format utils)
- [ ] Данные с `GET /api/cards/by-user` → store / пропсы
- [ ] Подписка на store: баланс обновляется без ручного reload страницы

### Блок B — Transactions

- [ ] Список + `TransactionItem`
- [ ] Типы: Income vs Expense как в [api.md](../api.md)

### Блок C — Actions

- [ ] Кнопки top-up / withdrawal (модалки или простые формы)
- [ ] После успеха — обновить карту в store

### Блок D — Statistics *(полная версия)*

- [ ] `GET /api/statistics` → два значения income / expense

### Блок E — Contacts / Transfer *(полная версия)*

- [ ] Поиск пользователей, debounce
- [ ] Форма перевода + validation
- [ ] После transfer — новый balance и новая строка в списке

**MVP:** блоки A + B (+ C по желанию). D и E можно после полировки.

**Сдать:** по блоку — скрин + какой эндпоинт дергается.

---

## Server

- [ ] Эндпоинты cards / transactions уже из 0b
- [ ] Transfer в одной транзакции БД (`BEGIN` … `COMMIT`)
- [ ] Недостаточно средств → 400 `{ message: "Insufficient funds" }`
- [ ] По блокам D/E: statistics и users search отвечают по контракту

Баги баланса и двойных транзакций чиним **на сервере**, не костылями на клиенте.
